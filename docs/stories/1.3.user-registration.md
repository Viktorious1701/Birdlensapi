# Story 1.3: Secure User Registration

## Status
Done

## Story
**As a** Mobile User,
**I want** to register a new account securely
**so that** I can access the Birdlens community and save my preferences.

## Acceptance Criteria
1. A `POST /api/v1/auth/register` endpoint accepts a JSON payload containing `email`, `password`, and `username`.
2. The endpoint validates the input (e.g., valid email format, minimum password length).
3. The user's password is securely hashed using BCrypt before being saved to the database.
4. The endpoint returns a `201 Created` status with the new user's basic profile data (excluding the password hash).
5. If an email or username already exists, the endpoint returns a `409 Conflict` with an appropriate error message.
6. A Testcontainers-backed integration test verifies successful registration and duplicate rejection against a real PostgreSQL instance.

## Tasks / Subtasks
- [x] Task 1: Create User Domain (AC: 1, 3)
  - [x] Create `User` entity in `com.birdlens.api.domain.user`.
  - [x] Create `UserRepository` extending `JpaRepository`.
- [x] Task 2: Implement Registration Logic (AC: 2, 3, 5)
  - [x] Create `RegisterRequest` DTO with Validation annotations (`@Email`, `@NotBlank`, `@Size`).
  - [x] Create `UserResponse` DTO to return user data without the password.
  - [x] Create `UserService` to handle business logic: check for existing users, hash password, and save.
- [x] Task 3: Create Auth Controller (AC: 1, 4, 5)
  - [x] Create `AuthController` with `POST /api/v1/auth/register`.
  - [x] Use `ApiResponse` standard wrapper for the response.
- [x] Task 4: Configure Password Encoding (AC: 3)
  - [x] Define a `PasswordEncoder` bean (BCrypt) in `SecurityConfig`.
- [x] Task 5: Integration Testing (AC: 6)
  - [x] Create `AuthIntegrationTest` using Testcontainers.
  - [x] Verify successful registration flow and 409 Conflict flow.

## Dev Notes
- **Security**: The Architecture document mandates BCrypt with a strength factor of 12.
- **Package Structure**: Follow the architecture: `com.birdlens.api.domain.user`.
- **Response Format**: Use the standard `ApiResponse` shape defined in the Architecture:
  ```json
  {
    "success": true,
    "data": { ... user info ... },
    "timestamp": "..."
  }
  ```
- **Error Handling**: Use the `GlobalExceptionHandler` pattern mentioned in the architecture to return consistent error messages for `ConflictException`.

### Testing
- Use Postman or curl to test:
  `POST http://localhost:8088/api/v1/auth/register`
- Payload: `{"email": "test@example.com", "username": "tester", "password": "password123"}`
- Ensure a second attempt with the same email returns `409 Conflict`.

## Change Log
| Date | Version | Description | Author |
| :--- | :--- | :--- | :--- |
| 2026-03-25 | 1.0 | Initial story creation | BMad Scrum Master |

## Dev Agent Record
### Agent Model Used
BMad Dev Agent

### Debug Log References
Included Testcontainers dependencies in build.gradle to support AbstractIntegrationTest configuration.

### Completion Notes List
Implemented ApiResponse wrapper for consistent API formatting.
Created GlobalExceptionHandler and ConflictException to handle domain logic exceptions elegantly without leaking stack traces.
Built User entity, UserRepository, and UserService holding the BCrypt hashing logic.
Created AuthController handling RegisterRequest with @Valid constraints.
Built AuthIntegrationTest utilizing Testcontainers for full end-to-end database testing of the register route.

### File List
build.gradle (Modified)
src/main/java/com/example/birdlensapi/config/SecurityConfig.java (Modified)
src/main/java/com/example/birdlensapi/common/dto/ApiResponse.java (Created)
src/main/java/com/example/birdlensapi/common/exception/ConflictException.java (Created)
src/main/java/com/example/birdlensapi/common/exception/GlobalExceptionHandler.java (Created)
src/main/java/com/example/birdlensapi/domain/user/User.java (Created)
src/main/java/com/example/birdlensapi/domain/user/UserRepository.java (Created)
src/main/java/com/example/birdlensapi/domain/user/RegisterRequest.java (Created)
src/main/java/com/example/birdlensapi/domain/user/UserResponse.java (Created)
src/main/java/com/example/birdlensapi/domain/user/UserService.java (Created)
src/main/java/com/example/birdlensapi/domain/user/AuthController.java (Created)
src/test/java/com/example/birdlensapi/integration/AbstractIntegrationTest.java (Created)
src/test/java/com/example/birdlensapi/integration/AuthIntegrationTest.java (Created)

## QA Results
_TBD_
