# Story 2.4: Scheduled Spatial Hotspot Ingestion Job

## Status
Done

## Story
**As the** System,
**I want** a scheduled background job to ingest notable birding hotspots per region,
**so that** the mobile app can query them locally without hitting eBird.

## Acceptance Criteria
1. A Spring `@Service` (`HotspotIngestionJob`) containing an `@Scheduled` method runs periodically (e.g., every 6 hours).
2. The service iterates through a configured list of supported regions (e.g., `["US", "VN", "GB"]` loaded from `application.yml`).
3. For each region, the service calls the eBird `v2/data/obs/{regionCode}/recent/notable` endpoint via the `EbirdApiClient`.
4. The service maps the observations to unique `ebird_hotspots` records. (Note: Multiple observations may occur at the same hotspot; the job must group/deduplicate them by `locId` before inserting).
5. The service performs an "Upsert" using `JdbcTemplate.batchUpdate()` against the PostGIS database.
6. The service correctly maps the API's `lat` and `lng` floats into the PostGIS `GEOGRAPHY(Point, 4326)` column using the `ST_SetSRID(ST_MakePoint(?, ?), 4326)` SQL function.
7. The service updates the `latest_obs_dt` and increments/updates `num_species_all_time` for existing hotspots, and inserts new hotspots.
8. The service logs its progress and failures for each region processed.

## Tasks / Subtasks
- [x] Task 1: Update API Client and DTOs (AC: 3, 4)
  - [x] Add `app.ebird.supported-regions: US,VN,GB` to `application.yml`.
  - [x] Add `fetchRecentNotableObservations(String regionCode)` to `EbirdApiClient`.
  - [x] Create `EbirdObservationDto` to map the API response (`locId`, `locName`, `lat`, `lng`, `obsDt`).
- [x] Task 2: Build the Ingestion Job (AC: 1, 2, 4)
  - [x] Create `HotspotIngestionJob.java` in `com.example.birdlensapi.ingestion`.
  - [x] Inject the list of supported regions from application properties using `@Value`.
  - [x] Configure `@Scheduled` (e.g., `cron = "0 0 */6 * * *"` for every 6 hours).
  - [x] Implement deduplication logic: The API returns *observations* (birds seen), but we are saving *hotspots* (locations). Group the returned list by `locId`, taking the most recent `obsDt` for each hotspot.
- [x] Task 3: Implement PostGIS Batch Upsert (AC: 5, 6, 7, 8)
  - [x] Inject `JdbcTemplate`.
  - [x] Write the UPSERT query. Crucially, the `location` column must be populated using `ST_SetSRID(ST_MakePoint(?, ?), 4326)::geography` in the `VALUES` clause, passing `longitude` then `latitude`.
  - [x] Wrap the region loop in a try-catch so one failing region does not stop the others.

## Dev Notes
- **Deduplication Strategy**: Filtered out personal locations (which start with 'P') ensuring we only ingest public hotspots (which start with 'L'). 
- **Time/Date mapping**: Ebird returns `obsDt` as `2026-03-24 14:00`. Used basic string extraction `substring(0, 10)` to pull `yyyy-MM-dd` since PostGIS `latest_obs_dt` accepts `java.sql.Date`.
- **PostGIS Syntax Mapping**: Strictly injected `longitude` as the 5th Prepared Statement parameter, and `latitude` as the 6th, preserving the expected `X, Y` coordinate map inside `ST_MakePoint`.

### Testing
- Fully vetted the unique deduplication collapse algorithm via Mockito in `HotspotIngestionJobTest`. Asserted that `L` prefixed spots merge securely, preferring the max date-time, and that `P` personal spots are discarded. Tested positional accuracy of `ST_MakePoint` parameters.

## Change Log
| Date | Version | Description | Author |
| :--- | :--- | :--- | :--- |
| Current | 1.0 | Initial story creation | BMad Scrum Master |
| Current | 1.1 | Implemented deduplication logic and PostGIS upserts | BMad Dev Agent |

## Dev Agent Record
### Agent Model Used
BMad Dev Agent

### Debug Log References
- Addressed personal location leakage by aggressively filtering raw payloads for `locId.startsWith("L")` preventing user-specific yard locations from muddying the public hotspot list.

### Completion Notes List
- Expanded `application.yml` allowing easy iteration across the three target regions (US, VN, GB).
- Integrated `fetchRecentNotableObservations` into `EbirdApiClient` relying safely on the previously established Resilience4j failover.
- Devised `HotspotIngestionJob` utilizing `0 0 */6 * * *` standard cron syntax for 6-hour polling intervals.
- Protected the iteration loop with an inner `try-catch`, ensuring one bad regional API outage does not stop execution of the rest of the list.
- Passed down custom `java.sql.Date` casting, securing our DB mapping into the `DATE` column format defined originally in Flyway.

### File List
- `src/main/resources/application.yml` (Modified)
- `src/main/java/com/example/birdlensapi/ingestion/EbirdObservationDto.java` (Created)
- `src/main/java/com/example/birdlensapi/ingestion/EbirdApiClient.java` (Modified)
- `src/main/java/com/example/birdlensapi/ingestion/HotspotIngestionJob.java` (Created)
- `src/test/java/com/example/birdlensapi/unit/HotspotIngestionJobTest.java` (Created)
- `docs/stories/2.4.scheduled-spatial-hotspot-ingestion.md` (Created)

## QA Results
_TBD_