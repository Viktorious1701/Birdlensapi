# Story 2.1: Define Bird Taxonomy and Spatial Hotspot Schemas

## Status
Done

## Story
**As a** Backend Developer,
**I want** to define the core database schemas for Bird Taxonomy and Spatial Hotspots,
**so that** ingested eBird data can be queried efficiently by the mobile application.

## Acceptance Criteria
1. A Flyway migration script creates a `bird_taxonomy` table (species_code, common_name, scientific_name, category, taxon_order, bird_order, family_common_name, family_scientific_name).
2. A Flyway migration script creates an `ebird_hotspots` table (loc_id, loc_name, country_code, subnational1_code, subnational2_code, latitude, longitude, latest_obs_dt, num_species_all_time).
3. The `latitude` and `longitude` columns are mapped to a PostGIS `GEOGRAPHY(Point, 4326)` column named `location` for efficient spatial querying.
4. Appropriate indexes (e.g., a GiST index on the `location` column) are created to optimize bounding box and radius queries.
5. Spring Data JPA entities are created and accurately mapped to these tables.

## Tasks / Subtasks
- [x] Task 1: Create Flyway Migration (AC: 1, 2, 3, 4)
  - [x] Create `V2__init_taxonomy_hotspots.sql` in `src/main/resources/db/migration/`.
  -[x] Define the `bird_taxonomy` table and add B-Tree indexes on `common_name` and `scientific_name` using `text_pattern_ops` (per architecture docs).
  - [x] Define the `ebird_hotspots` table including the `GEOGRAPHY(Point, 4326)` column.
  - [x] Create a GiST index on the `location` column of `ebird_hotspots`.
- [x] Task 2: Create JPA Entities (AC: 5)
  -[x] Add Hibernate Spatial dependency to `build.gradle` if not already present.
  - [x] Create `BirdTaxonomy` entity in `com.example.birdlensapi.domain.taxonomy` with `@Id` mapped to `species_code`.
  - [x] Create `EbirdHotspot` entity in `com.example.birdlensapi.domain.hotspot` with `@Id` mapped to `loc_id`.
  - [x] Map the spatial column in `EbirdHotspot` using `@Column(columnDefinition = "GEOGRAPHY(Point, 4326)")` and the `org.locationtech.jts.geom.Point` type.
- [x] Task 3: Create Repositories (AC: 5)
  - [x] Create `TaxonomyRepository` extending `JpaRepository`.
  - [x] Create `HotspotRepository` extending `JpaRepository`.

## Dev Notes
- **Architecture Reference**: The exact SQL definitions for these tables are located in `docs/architecture/data-models.md`. Use those precise definitions to ensure the GiST and text pattern indexes are created correctly.
- **Dependencies**: To work with PostGIS types in Java, `org.hibernate.orm:hibernate-spatial` was added to `build.gradle`.
- **Entity IDs**: These tables do *not* use UUIDs. `bird_taxonomy` uses `species_code` (VARCHAR) as its primary key, and `ebird_hotspots` uses `loc_id` (VARCHAR). `@GeneratedValue` was successfully omitted.

### Testing
- Executed the Spring Boot application locally; Flyway applied `V2__init_taxonomy_hotspots.sql` seamlessly.
- JPA successfully booted and validated (`ddl-auto: validate`) the mapping between `org.locationtech.jts.geom.Point` and `GEOGRAPHY(Point, 4326)`.

## Change Log
| Date | Version | Description | Author |
| :--- | :--- | :--- | :--- |
| Current | 1.0 | Initial story creation | BMad Scrum Master |
| Current | 1.1 | Story completed and marked as Done | BMad Dev Agent |

## Dev Agent Record
### Agent Model Used
BMad Dev Agent

### Debug Log References
- No errors encountered. Flyway correctly parsed the script and `hibernate-spatial` matched the types without configuration issues.

### Completion Notes List
- Applied `hibernate-spatial` into `build.gradle` to give Java natively mapping support for spatial types like `Point`.
- Generated `V2__init_taxonomy_hotspots.sql` creating the schemas verbatim from `data-models.md`. Indexes included exactly as specified.
- Created `BirdTaxonomy` entity using its `speciesCode` as the Primary ID, honoring the eBird API payload format.
- Created `EbirdHotspot` entity ensuring `@Column(columnDefinition = "GEOGRAPHY(Point, 4326)")` strictly locked in the SRID and geographical bounds of the point representing each hotspot.
- Implemented standard `JpaRepository` interfaces for both domains.

### File List
- `build.gradle` (Modified)
- `src/main/resources/db/migration/V2__init_taxonomy_hotspots.sql` (Created)
- `src/main/java/com/example/birdlensapi/domain/taxonomy/BirdTaxonomy.java` (Created)
- `src/main/java/com/example/birdlensapi/domain/taxonomy/TaxonomyRepository.java` (Created)
- `src/main/java/com/example/birdlensapi/domain/hotspot/EbirdHotspot.java` (Created)
- `src/main/java/com/example/birdlensapi/domain/hotspot/HotspotRepository.java` (Created)
- `docs/stories/2.1.define-taxonomy-hotspot-schemas.md` (Created)

## QA Results
_TBD_