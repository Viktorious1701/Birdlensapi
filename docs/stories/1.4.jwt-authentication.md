# Story 1.4: JWT-based Authentication (Login)

## Status Done

## Story
**As a** Mobile User,
**I want** to log in with my email and password to receive a secure session token (JWT)
**so that** I can authenticate my future API requests.

## Acceptance Criteria
1. Spring Security is configured to support stateless, JWT-based authentication.
2. A `POST /api/v1/auth/login` endpoint accepts `email` and `password`.
3. Upon successful credential verification, the endpoint returns a `200 OK` containing a short-lived `access_token` (JWT) and a long-lived `refresh_token`.
4. The `access_token` payload includes the user's ID and role.
5. Invalid credentials return a `401 Unauthorized` status.
6. A `JwtAuthFilter` is implemented to extract and validate tokens from the `Authorization: Bearer` header on subsequent requests.

## Tasks / Subtasks
- [x] Task 1: Implement JWT Utility (AC: 3, 4)
  - [x] Add `jjwt-api`, `jjwt-impl`, and `jjwt-jackson` to `build.gradle` (Version 0.12.x per architecture).
  - [x] Create `JwtService` to handle token generation, extraction, and validation.
  - [x] Ensure the access token includes `userId` and `roles` as claims.
- [x] Task 2: Create Login DTOs (AC: 2, 3)
  - [x] Create `LoginRequest` (email, password).
  - [x] Create `AuthResponse` (accessToken, refreshToken, user).
- [x] Task 3: Implement Login Logic (AC: 2, 3, 5)
  - [x] Add `login` method to `AuthService`.
  - [x] Use `AuthenticationManager` to verify credentials.
  - [x] Return `AuthResponse` on success; `GlobalExceptionHandler` handles 401 via `BadCredentialsException`.
- [x] Task 4: Implement JWT Security Filter (AC: 6)
  - [x] Create `JwtAuthFilter` extending `OncePerRequestFilter`.
  - [x] Configured to look for `Bearer` tokens in the `Authorization` header.
  - [x] If valid, sets the `SecurityContextHolder` with the authenticated principal.
- [x] Task 5: Finalize Security Configuration (AC: 1, 6)
  - [x] `JwtAuthFilter` added before `UsernamePasswordAuthenticationFilter` in `SecurityConfig`.
  - [x] Session Creation Policy set to `STATELESS`.
  - [x] `AuthenticationManager` bean defined in `SecurityConfig`.
- [x] Task 6: Integration Testing (AC: 5)
  - [x] `AuthIntegrationTest` updated to verify the full login flow.
  - [x] Verifies successful login returns `accessToken` and `refreshToken`.
  - [x] Verifies wrong password returns `401 Unauthorized` with `INVALID_CREDENTIALS` code.
  - [x] Verifies accessing a protected endpoint (`/api/v1/users/me`) without a token returns `401`.

## Dev Notes
- **JWT Version**: `io.jsonwebtoken:jjwt:0.12.5`. Uses `Jwts.builder().subject(...)` and `signWith(SecretKey)` — the API that changed in 0.12.0.
- **Statelessness**: `HttpSession` is not used. Security context is rebuilt from the JWT on every request via `JwtAuthFilter`.
- **Refresh Tokens**: Implemented as a longer-lived JWT (7 days) with subject only — no extra claims. Simpler for MVP; no database storage needed.
- **Access Token Claims**: `userId` (UUID as String) and `roles` (List of authority strings) are embedded as custom claims alongside the standard `subject` (email).
- **Property Keys**: JWT config is read from `app.jwt.secret`, `app.jwt.expiry-minutes`, `app.jwt.refresh-expiry-days` as defined in `application.yml`.
- **Environment Variables**: `JWT_SECRET` must be at least 256 bits. A safe local default is set in `application.yml` for dev only.
- **Bug Fixed**: `GlobalExceptionHandler` had duplicate `@ExceptionHandler` declarations for `ConflictException`, `MethodArgumentNotValidException`, and `Exception` which would cause a Spring startup failure. All duplicates were removed.

### Testing
- `POST http://localhost:8088/api/v1/auth/login`
- Body: `{"email": "test@example.com", "password": "password123"}`
- Expect: `{"success": true, "data": {"accessToken": "...", "refreshToken": "...", "user": {...}}, ...}`

## Change Log
| Date | Version | Description | Author |
| :--- | :--- | :--- | :--- |
| 2026-03-25 | 1.0 | Initial story creation | BMad Scrum Master |
| 2026-03-27 | 1.1 | Story implemented and marked as Done | BMad Dev Agent |

## Dev Agent Record
### Agent Model Used
BMad Dev Agent

### Debug Log References
- Fixed duplicate `@ExceptionHandler` mappings in `GlobalExceptionHandler` that would have caused a Spring startup failure.
- Fixed JWT property keys to match `application.yml` prefix (`app.jwt.*` instead of `jwt.*`).

### Completion Notes List
- `JwtService` updated: `generateAccessToken` now embeds `userId` (UUID as String) and `roles` (list of authority strings) as custom JWT claims. `generateRefreshToken` remains minimal (subject + expiry only). Added `extractUserId` and `extractRoles` helper methods.
- `GlobalExceptionHandler` fixed: removed four duplicate `@ExceptionHandler` declarations, leaving one clean handler per exception type.
- `AuthIntegrationTest` updated: added `shouldLoginSuccessfullyAndReturnTokens`, `shouldReturn401OnInvalidCredentials`, and `shouldReturn401WhenAccessingProtectedEndpointWithoutToken`.

### File List
- `src/main/java/com/example/birdlensapi/security/JwtService.java` (Modified)
- `src/main/java/com/example/birdlensapi/common/exception/GlobalExceptionHandler.java` (Modified)
- `src/test/java/com/example/birdlensapi/integration/AuthIntegrationTest.java` (Modified)

## QA Results
_TBD_