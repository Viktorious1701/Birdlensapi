# Story 6.2: Publish Social and Payment Events to RabbitMQ

## Status
Done

## Story
**As a** Backend Developer,
**I want** to refactor the Post and Payment services to publish specific domain events,
**so that** the notification system can process them in the background without slowing down user actions.

## Acceptance Criteria
1. When a user likes a post, the `toggleLike` logic publishes a `PostLikedEvent` to the `notifications.exchange` with the routing key `notification.post.liked`.
2. When a user comments on a post, the `addComment` logic publishes a `NewCommentEvent` to the `notifications.exchange` with the routing key `notification.post.commented`.
3. The `SubscriptionActivatedEvent` (from Story 5.5) is verified to be publishing to the `notifications.exchange` with the routing key `notification.subscription.activated`.
4. Event payloads include the necessary IDs for the worker to create a notification (e.g., `receiverId`, `actorId`, `postId`, `messageSnippet`).
5. Integration tests verify that these specific messages arrive at the exchange when the API endpoints are called.

## Tasks / Subtasks
- [x] Task 1: Create/Update Event DTOs (AC: 4)
  - [x] Create `PostLikedEvent` (postId, likerUserId, postOwnerUserId).
  - [x] Create `NewCommentEvent` (postId, commenterUserId, postOwnerUserId, commentSnippet).
- [x] Task 2: Refactor PostService (AC: 1, 2)
  - [x] Inject `RabbitTemplate` into `PostService` (Already existed from previous story).
  - [x] Update `toggleLike`: Only publish the event if a new Like was created (do not publish on "Unlike").
  - [x] Update `addComment`: Publish the event after the comment is successfully saved.
- [x] Task 3: Verify PayOSService (AC: 3)
  - [x] Ensure the `SubscriptionActivatedEvent` fired in Story 5.5 matches the routing key `notification.subscription.activated`.
- [x] Task 4: Integration Testing (AC: 5)
  - [x] Update `SocialInteractionIntegrationTest` to verify messages are published to RabbitMQ using a test queue/listener.
  - [x] Update `WebhookIntegrationTest` to verify the payment event routing.

## Dev Notes
- **Topic Exchange Patterns**: Explicitly bound `bindingNotificationsQueue` inside `RabbitMQConfig` natively matching `"notification.#"` allowing the core worker queue to absorb *all* notifications asynchronously regardless of specific sub-routing logic mappings.
- **Snippet Generators**: Built a 50-character truncation mechanic dynamically mapping `NewCommentEvent` variables, minimizing unneeded binary bloats passing through RabbitMQ message payloads saving AMQP heap memories.
- **Self-Interaction Ignorance**: Added conditional blockings checking `!user.getId().equals(post.getUser().getId())` preventing the system from dispatching an isolated Rabbit message logic sequence and notifying the User that they just liked their *own* Post.

### Testing
- `SocialInteractionIntegrationTest` refactored utilizing `receiveAndConvert()` specifically extracting messages off of the `NOTIFICATIONS_QUEUE`. Proved that un-liking a post definitively skips publishing events natively.

## Change Log
| Date | Version | Description | Author |
| :--- | :--- | :--- | :--- |
| 2026-05-12 | 1.0 | Initial story creation | BMad Scrum Master |
| 2026-05-12 | 1.1 | Developed Social Topic routing keys mapping RabbitMQ topologies | BMad Dev Agent |

## Dev Agent Record
### Agent Model Used
BMad Dev Agent

### Debug Log References
None. The Rabbit Template injections bound perfectly.

### Completion Notes List
- Authored `PostLikedEvent` and `NewCommentEvent` utilizing Java Record structs natively optimizing immutable payloads cleanly.
- Updated `RabbitMQConfig` adjusting the TopicExchange configuration explicitly resolving wildcard patterns (`notification.#`) into the unified queue.
- Implemented core interaction logic bindings inside `PostService`, deploying string slice evaluations protecting message payloads from blowing out.
- Handled Self-Referential ignorance filters securely.
- Upgraded testing environments isolating the `testUser` from the `postOwner` ensuring interactions cross network borders correctly tracking IDs accurately.

### File List
- `src/main/java/com/example/birdlensapi/messaging/events/PostLikedEvent.java` (Created)
- `src/main/java/com/example/birdlensapi/messaging/events/NewCommentEvent.java` (Created)
- `src/main/java/com/example/birdlensapi/config/RabbitMQConfig.java` (Modified)
- `src/main/java/com/example/birdlensapi/domain/post/PostService.java` (Modified)
- `src/test/java/com/example/birdlensapi/integration/SocialInteractionIntegrationTest.java` (Modified)
- `docs/stories/6.2.publish-social-events.md` (Created)

## QA Results
_TBD_