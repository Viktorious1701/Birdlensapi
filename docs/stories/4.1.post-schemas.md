# Story 4.1: Define Post, Media, and Interaction Schemas

## Status
Done

## Story
**As a** Backend Developer,
**I want** to create the database schemas and JPA entities for Posts, Comments, and Reactions,
**so that** I can store community-generated content and media metadata securely.

## Acceptance Criteria
1. A Flyway migration creates a `posts` table (id, user_id, content, location_name, location_point, privacy_level, type, sighting_date, tagged_species_code, created_at, updated_at).
2. A Flyway migration creates a `post_media` table (id, post_id, original_url, thumbnail_url, compressed_url, processing_status, created_at).
3. A Flyway migration creates a `post_comments` table (id, post_id, user_id, content, created_at).
4. A Flyway migration creates a `post_reactions` table (id, post_id, user_id, reaction_type, created_at).
5. JPA entities (`Post`, `PostMedia`, `PostComment`, `PostReaction`) are created and correctly mapped to these tables.
6. Entity relationships are configured correctly (e.g., `Post` has many `PostMedia` mapped by `post`).

## Tasks / Subtasks
- [x] Task 1: Create Flyway Migration (AC: 1, 2, 3, 4)
  - [x] Create `V3__init_posts_media_reactions.sql` in `src/main/resources/db/migration/`.
  - [x] Define the 4 tables exactly as outlined in the `docs/architecture/data-models.md` file.
    -[x] Ensure `id` columns use `UUID PRIMARY KEY DEFAULT gen_random_uuid()`.
  -[x] Ensure `location_point` in `posts` uses `GEOGRAPHY(Point, 4326)`.
  - [x] Add the specified indexes (e.g., `idx_posts_user_id`, `idx_posts_created_at`).
- [x] Task 2: Create JPA Entities (AC: 5, 6)
  - [x] Create `Post`, `PostMedia`, `PostComment`, and `PostReaction` in `com.example.birdlensapi.domain.post`.
  -[x] Map `location_point` using `@Column(columnDefinition = "GEOGRAPHY(Point, 4326)")` and `org.locationtech.jts.geom.Point`.
  - [x] Define enums: `PrivacyLevel` (PUBLIC, PRIVATE, FOLLOWERS_ONLY), `PostType` (GENERAL, SIGHTING), `ProcessingStatus` (PENDING, PROCESSING, COMPLETED, FAILED), and `ReactionType` (LIKE, LOVE, WOW).
  - [x] Map enums using `@Enumerated(EnumType.STRING)`.
  - [x] Create `@ManyToOne` relationships to `User` (and `BirdTaxonomy` where applicable), and `@OneToMany` from `Post` to `PostMedia` (use `cascade = CascadeType.ALL` and `orphanRemoval = true`).
- [x] Task 3: Create Repositories
  - [x] Create `PostRepository`, `PostMediaRepository`, `PostCommentRepository`, and `PostReactionRepository`.

## Dev Notes
- **Constraint Implementations**: Introduced a `UNIQUE(post_id, user_id, reaction_type)` constraint on the `post_reactions` table natively inside the Flyway migration. This prevents users from liking the same post multiple times at the database layer.
- **JPA Cascades**: Specifically mapped `cascade = CascadeType.ALL` and `orphanRemoval = true` onto `Post.media`. When a post is deleted via the Repository, the associated media entities will be purged concurrently.
- **Enums**: Explicitly marked all Enums with `@Enumerated(EnumType.STRING)` to protect database integrity against Java-side reordering.

### Testing
- Validated via `ddl-auto: validate` confirming the Spring Boot runtime maps perfectly into the `V3` initialized tables.

## Change Log
| Date | Version | Description | Author |
| :--- | :--- | :--- | :--- |
| 2026-04-14 | 1.0 | Initial story creation | BMad Scrum Master |
| 2026-04-14 | 1.1 | Developed schemas, entities, enums, and repo interfaces | BMad Dev Agent |

## Dev Agent Record
### Agent Model Used
BMad Dev Agent

### Debug Log References
None. Entities mapped and validated against Flyway scripts flawlessly.

### Completion Notes List
- Formulated `V3__init_posts_media_reactions.sql` containing 4 exact DDL table schemas, adhering strictly to PostGIS data types (`GEOGRAPHY`).
- Established 4 domain Enums to safely track status and typing configurations.
- Engineered 4 JPA domain Entities properly leveraging `FetchType.LAZY` bindings for optimized payload fetching.
- Added bidirectional synchronization helper methods (`addMedia`, `removeMedia`) on the `Post` root aggregate entity to maintain clear JPA cache boundaries.
- Bootstrapped 4 Spring Data Repositories for the Post module ready for immediate implementation.

### File List
- `src/main/resources/db/migration/V3__init_posts_media_reactions.sql` (Created)
- `src/main/java/com/example/birdlensapi/domain/post/PrivacyLevel.java` (Created)
- `src/main/java/com/example/birdlensapi/domain/post/PostType.java` (Created)
- `src/main/java/com/example/birdlensapi/domain/post/ProcessingStatus.java` (Created)
- `src/main/java/com/example/birdlensapi/domain/post/ReactionType.java` (Created)
- `src/main/java/com/example/birdlensapi/domain/post/Post.java` (Created)
- `src/main/java/com/example/birdlensapi/domain/post/PostMedia.java` (Created)
- `src/main/java/com/example/birdlensapi/domain/post/PostComment.java` (Created)
- `src/main/java/com/example/birdlensapi/domain/post/PostReaction.java` (Created)
- `src/main/java/com/example/birdlensapi/domain/post/PostRepository.java` (Created)
- `src/main/java/com/example/birdlensapi/domain/post/PostMediaRepository.java` (Created)
- `src/main/java/com/example/birdlensapi/domain/post/PostCommentRepository.java` (Created)
- `src/main/java/com/example/birdlensapi/domain/post/PostReactionRepository.java` (Created)
- `docs/stories/4.1.define-post-media-interaction-schemas.md` (Created)

## QA Results
_TBD_