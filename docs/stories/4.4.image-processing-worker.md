# Story 4.4: Asynchronous Image Processing Worker

## Status
Draft

## Story
**As the** System,
**I want** a dedicated background worker to consume PostCreatedEvent messages and process the associated images,
**so that** users have fast-loading thumbnails and optimized images for their feed.

## Acceptance Criteria
1. A `@RabbitListener` is implemented to consume messages from the `image-processing-queue`.
2. The consumer is only active when the Spring profile `worker` is enabled.
3. For each `postId` in the event, the worker fetches the associated `post_media` records.
4. The worker downloads the original image from MinIO/S3.
5. The worker generates a 300x300 thumbnail and a web-compressed (quality ~70%) version of each image using a library like `Thumbnailator`.
6. The worker uploads the processed versions back to MinIO/S3 and updates the `post_media` record with the new URLs and sets status to `COMPLETED`.
7. Failed processing attempts are retried 3 times with backoff before being sent to a Dead-Letter Queue (DLQ).
8. An integration test verifies the full async flow: publish event -> worker processes -> DB record status is COMPLETED.

## Tasks / Subtasks
- [ ] Task 1: Add Image Processing Library (AC: 5)
  - [ ] Add `net.coobird:thumbnailator:0.4.20` to `build.gradle`.
- [ ] Task 2: Configure Worker Profile (AC: 2, 7)
  - [ ] Update `RabbitMQConfig` to define a Dead-Letter Exchange (`dlx.exchange`) and Queue (`image-processing-dlq`).
  - [ ] Configure the `RetryInterceptor` with 3 attempts and 2s backoff.
- [ ] Task 3: Implement the Consumer (AC: 1, 3, 4, 5, 6)
  - [ ] Create `ImageProcessingConsumer.java` in `com.example.birdlensapi.messaging.consumers`.
  - [ ] Annotate the class with `@Profile("worker")`.
  - [ ] Implement the `@RabbitListener(queues = "image-processing-queue")` method.
  - [ ] Logic: Fetch Media -> Download (via `S3StorageService`) -> Resize -> Upload -> Update DB.
- [ ] Task 4: Enhance Storage Service (AC: 4, 6)
  - [ ] Add `downloadFile(String key)` and `uploadFile(String key, byte[] content, String contentType)` to `S3StorageService`.
- [ ] Task 5: Integration Testing (AC: 8)
  - [ ] Create `ImageProcessingIntegrationTest`.
  - [ ] Use `Awaitility` to wait for the background worker to finish before asserting that the database status changed from `PENDING` to `COMPLETED`.

## Dev Notes
- **Library Selection**: `Thumbnailator` is recommended in the architecture for its simplicity and small footprint.
- **Async Testing**: Since the worker runs in a separate thread, your test needs to wait. Add `org.awaitility:awaitility` to `testImplementation`.
- **Memory Management**: When processing images in a worker, ensure you are not loading massive files into memory all at once if there are many images in a post. Use `InputStream` where possible.
- **DLQ Setup**: If the worker throws an exception after 3 retries, RabbitMQ should move the message to the DLQ automatically.

### Testing
- Test file location: `src/test/java/com/example/birdlensapi/integration/ImageProcessingIntegrationTest.java`
- Verify that both the `thumbnail_url` and `compressed_url` fields in the database are no longer null after processing.

## Change Log
| Date | Version | Description | Author |
| :--- | :--- | :--- | :--- |
| Current | 1.0 | Initial story creation | BMad Scrum Master |

## Dev Agent Record
### Agent Model Used
_TBD_

### Debug Log References
_TBD_

### Completion Notes List
_TBD_

### File List
_TBD_

## QA Results
_TBD_